# Server
NODE_ENV=development
PORT=8080
# 6-digit PIN used to access the host / admin area of the app
ADMIN_PIN=000000
# Where the SQLite database file lives (relative to server/)
DB_PATH=./data/oscar-arena.db
